package view.components;

import javax.swing.*;

public class SideBar extends JPanel {

    public SideBar() {
        setBackground(Palette.mainBlue());

        // Positioning widgets
        setLayout(null);
    }
}
