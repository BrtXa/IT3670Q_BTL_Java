package view.components;

import java.awt.*;
import javax.swing.*;

public class AppBar extends JPanel {
    public AppBar() {
        // Make this a horizontal appbar with red color
        setBackground(new Color(237, 10, 0));
        setPreferredSize(new Dimension(929, 215));
    }
}
